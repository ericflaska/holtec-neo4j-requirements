# PDF & images
pymupdf>=1.24.0
Pillow>=10.0.0

# Model inference (Qwen VL)
torch>=2.0.0
transformers>=4.45.0
accelerate>=0.25.0
safetensors>=0.4.0

# Neo4j
neo4j>=5.15.0

# Data validation & config
pydantic>=2.0.0
python-dotenv>=1.0.0

# Nova Pro tier classification (Bedrock). 1.34.43+ has converse(); older uses invoke_model fallback.
boto3>=1.34.0

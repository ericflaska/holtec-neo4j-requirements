# Credentials and secrets — never commit
.env
.env.local
.env.*.local
*.pem
aws_env_lines.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Large generated data (embeddings, checkpoints)
data/
requirements_checkpoint.json
requirements_checkpoint_embed_tier.json
